# Bronze Notebooks

Bronze is the raw landing layer. The ADF pipeline performs the primary source-to-Bronze ingestion.

Databricks notebooks in this folder are reserved for Bronze-side Spark ingestion where required.
