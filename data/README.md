# Data

Raw AdventureWorks data files are not committed to this repository.

The project stores and processes data in Azure storage.

This keeps the repository lightweight and avoids unnecessarily redistributing source datasets.
